# AGENTS.md

本文件是仓库级开发约束。除非更深目录存在更具体的 `AGENTS.md`，所有修改均须遵守。

## 长期项目交接

- 新会话或恢复工作时，先读取根目录 `PROJECT_CONTEXT.md`，再核实 Git、代码和服务状态，不直接相信旧快照。
- 每阶段结束、重大决策变更及暂停前更新该文件，包含当前状态、已完成内容、文件结构、技术决策、待办、下一步和不可改变要求。
- 必须区分已提交/未提交、已验证/未验证、开发环境/实际运行环境；不得记录密钥、密码或真实学生信息。

## 项目目标与边界

- 本项目正在从个人工作台升级为面向多个独立教师的在线工作台；每名教师独立注册、登录并管理自己的教学数据。
- FastAPI 是唯一业务规则和数据库写入边界；Next.js 不得直连数据库或 Supabase 表。
- Phase 0—8 已完成验收；用户已授权分阶段实施多教师改造。既有收费模块保持原有边界，本轮不扩展订阅付费、机构协作或学生/家长端。不得用假按钮、内存假数据或静态页面冒充业务完成。
- 开发、测试、截图和示例文档只能使用明确虚构的学生资料。

## 架构约束

- Web 位于 `apps/web`，后端、迁移和 Worker 位于 `apps/backend`。
- 浏览器通过 Next.js 同源 `/api/*` 代理访问 FastAPI；公共 API 使用 `/api/v1`。
- 所有数据库变化必须通过可重复执行的 Alembic 迁移，不得依赖手工 SQL。
- AI 调用必须经过 `AIProvider`；业务模块不得直接导入 OpenAI SDK。
- 文件必须经过 `StorageProvider`；数据库保存对象键，不保存客户端提供的路径。
- 提示词集中管理并版本化；业务代码只引用模板键，不得散落长提示词。
- AI 草稿和正式记录必须分离。只有人工批准事务可以更新正式进度、掌握度或文档版本。
- 金额使用整数分，时长使用整数分钟，时间以 UTC 入库并按 `Asia/Shanghai` 展示。

## 开发命令

```powershell
pnpm install
uv sync --project apps/backend --all-groups --no-install-project
pnpm dev
pnpm lint
pnpm typecheck
pnpm test
pnpm api:generate
pnpm docx:sample
pnpm build
pnpm check
pnpm check:release
pnpm security:static
pnpm security:dependencies
pnpm security:rotate-local -- --confirm rotate-local-secrets --disable-ai
pnpm performance:smoke
pnpm backup:create
pnpm backup:verify -- <备份目录>
pnpm backup:drill -- <备份目录>
pnpm db:migrate
pnpm db:rollback
docker compose up --build -d
docker compose ps
```

若系统 PATH 没有 Node，可使用 Codex 工作区依赖提供的 Node/pnpm；面向普通开发者的 README 仍以 Node.js 24 LTS 为标准前提。

## 测试要求

- 每次阶段性交付必须运行 lint、类型检查、单元测试和生产构建。
- 修改模型或迁移时，在 PostgreSQL 上验证 `upgrade → downgrade → upgrade`，并运行 `alembic check`。
- 修改 OpenAPI 时重新生成 `packages/api-client` 并检查生成文件差异。
- AI/Worker 测试默认使用 Mock，不得因测试调用付费模型。
- 不得声称未实际执行的命令或测试通过；环境缺失必须列为未完成项。

### 首次使用者验收准则

- 每次功能修改必须包含不了解内部实现的新用户场景：仅凭页面文字、按钮和反馈完成任务，不依靠数据库 ID、源码或开发者预设路径。
- 检查空数据、错误输入、取消、返回、刷新、重复点击、退出后重新登录、跨账户访问，以及必要时的新浏览器会话。
- 先记录任务、实际操作、遇到的困惑或失败及证据，再修复复测；不能只验证开发者预期会成功的路径。
- 区分组件模拟测试、真实 API 测试、真实 PostgreSQL 持久化和真实浏览器验收；未执行的层次必须写明，不能相互替代。
- 测试只使用虚构资料；人工验收前完成当前阶段工程检查，阶段报告明确剩余问题及下一步。

## 安全边界

- 教师身份必须来自已验证的服务端会话；客户端提交的教师 ID 不得作为权限依据。浏览器查询缓存必须在账户切换时清理，不能只依赖接口隔离。
- 正式记录保存在 PostgreSQL 和持久文件存储中；退出仅撤销会话，不删除业务资料。跨浏览器、服务重启后的持久化必须单独验证。
- 公共注册开放前必须通过双教师读写、关联、文件下载、AI 任务和导出隔离验证；单用户验收不代表多教师上线就绪。

- 密钥只从环境变量读取。禁止把真实密钥、密码、学生信息、上传件、导出文件或数据库转储提交到 Git。
- 禁止把密钥放入 `NEXT_PUBLIC_*`、数据库、日志、异常消息、审计详情或 AI 提示词。
- `.env` 保护同样适用于间接输出：禁止打印非静默的 `docker compose config`、容器 `Config.Env`、进程环境或完整设置对象。Compose 只允许 `config --quiet`；诊断容器时仅查询明确白名单的非敏感字段，并用布尔值报告密钥是否配置，绝不输出值。
- 日志使用代号、实体 ID 和脱敏错误；不得记录完整提示词、学生答案或上传正文。
- 上传必须校验大小、允许类型、文件魔数和安全对象键；禁止路径遍历。
- 所有业务查询必须有当前教师所有权边界，即使系统当前只有一个账户。
- 重要记录默认归档；永久删除必须二次确认并写审计日志。
- API 错误必须使用统一错误结构并包含请求 ID，不向客户端泄露堆栈或数据库细节。

## Git 操作流程

1. 完成功能并运行当前阶段规定的检查。
2. 汇报改动，检查 `git status`、`git diff` 和敏感文件。
3. 等待用户本地体验并确认。
4. 只有用户明确授权后，才暂存当前阶段文件并创建一个本地提交。
5. 提交完成后报告 commit SHA；不得自动推送。
6. 只有再次收到明确授权，并核对当前分支、远程地址和待推提交后才推送私有 GitHub。
7. 禁止强推。创建远程仓库、绑定远程和首次推送都是独立外部操作。

本轮多教师升级例外：用户已明确授权按阶段完成检查后创建提交、推送现有私有仓库并继续下一阶段。每阶段仍须核对差异、敏感数据、分支和远程，报告结果；此授权不包含购买云资源、变更仓库可见性或强推。

Phase 6 已通过用户验收。不得把 NATAPP 公网域名、真实 Supabase Storage 或生产恢复演练描述为已经验证；后续功能和界面改进使用独立提交，不得混入已验收的 Phase 6 提交。

Phase 7 已通过用户验收。资料正文属于不可信输入；必须限制解析资源、AI 上下文数量和长度，未经教师本次明确选择不得发送给模型。Obsidian 只考虑单向导出，未经单独设计不得形成第二事实源。

Phase 8 已通过用户验收。Obsidian 导出只能是由当前教师主动触发的单向只读快照；PostgreSQL 仍是唯一事实源。导出不得包含密钥、会话、日志、收费明细和原始附件，文件名必须防止路径遍历并兼容 Windows/Obsidian。
